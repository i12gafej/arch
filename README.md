# Arch Studio Monorepo

This repo contains:
- packages/arch-core: pure compiler (graph -> AST -> IR -> plan)
- packages/arch-workspace: IO contracts
- packages/arch-adapter-fs: filesystem adapter
- apps/arch-studio: desktop UI

Use the CLI in packages/arch-cli for now.
