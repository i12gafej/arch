{
  "id": "ffmpeg",
  "version": "0.1",
  "env": [
    {"key": "FFMPEG_PATH", "value": "ffmpeg"}
  ],
  "compose": [],
  "settings": [
    "FFMPEG_PATH = \"ffmpeg\""
  ],
  "adapter": {
    "template": "templates/adapter.py",
    "prefix": "FFmpeg",
    "requiresPort": true
  },
  "doctorChecks": [
    {"type": "env", "key": "FFMPEG_PATH"},
    {"type": "binary", "command": "ffmpeg -version"}
  ]
}
